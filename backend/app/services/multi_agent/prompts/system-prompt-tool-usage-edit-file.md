## edit_file

- Use for writing text artifacts under `wence_data/project`.
- Create missing files when needed.
- Choose append vs overwrite deliberately.
- Never attempt writes outside project sandbox.
