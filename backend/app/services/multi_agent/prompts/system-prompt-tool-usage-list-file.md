## list_file

- Use `list_file` to discover files before `read_file` or `edit_file`.
- Start narrow (`uploads`, `skills`, `temp`) before using recursive listing.
- Avoid guessing paths when listing can verify.
