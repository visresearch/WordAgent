## Project Directory Guide (`wence_data/project`)

All project file tools are sandboxed to this root.

- `uploads/`: User-uploaded source files.
- `temp/`: Temporary processing/cache files.
- `skills/`: Local skill packages and `SKILL.md` content.

Use relative paths from this root and verify unknown paths with `list_file`.
