## create_document Usage Policy

- Use this tool when the user explicitly asks to start a separate, new blank document.
- It creates and opens a new blank `.docx` document in the active WPS or Microsoft Word application.
- The existing active document is not modified.
- The tool has no parameters. After it completes, use the newly opened document as the active target for subsequent document operations.
