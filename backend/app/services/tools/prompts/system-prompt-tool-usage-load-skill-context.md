## load_skill_context Usage Policy

- Use when the user names a skill/style standard or the task clearly needs reusable domain guidance.
- `skill_name` is the skill folder name; backend supports case-insensitive matching.
- After loading, follow the skill constraints. If missing, say so briefly and continue with best-effort defaults.
